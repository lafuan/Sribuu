{
  "name": "sribuu-worker",
  "version": "1.0.0",
  "description": "Sribuu - Personal finance tracker on Cloudflare Pages",
  "main": "_worker.ts",
  "scripts": {
    "build": "python3 build.py",
    "deploy": "npm run build && wrangler pages deploy . --project-name sribuu --branch main",
    "dev": "wrangler pages dev .",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:ci": "npm run type-check && npm run test",
    "check": "npm run type-check && npm run test && npm run test:e2e"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hono": "^4.11.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250706.0",
    "@playwright/test": "^1.61.1",
    "concurrently": "^10.0.3",
    "esbuild": "^0.24.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.9"
  }
}
